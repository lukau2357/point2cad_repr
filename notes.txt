- Can we construct arbitrary Euler cycle, without using angular ordering?

- Change visualization logic: green (survived arc), red (trimmed arc)? Maybe only green is enough?

- Repeated lines in --visualize, sampling bug or deeper intersection issues?

- If greedy removal fails, try augmenting?

- Do upsampling of the input, upsampling should preserve the underyling surface shape!

- Interleave analytical intersections with mesh intersections if OCC fails, produced ambiguous results...

- Cluster oversegmentation remains a problem, investigate how to solve it, preferably in the point cloud generation script!

- Parameter choices (spacing_factor = 1.5, score_cap = 10)
  - 00949 (default, default) (works with MSI). Google drive export: analytical + manual
  - 00470 (default, default) (freeform) Does not work with MSI. Google drive export: analytical + manual
  - 00000949 (default, default) (workst with MIS) Google drive export: analytical + manual
  - 00000005 (default, default) (tangent geometries present) Google drive export: analytical + manual. Fails for part 7, 9/10 success rate
  - 00000006 (defualt, default) (works with MSI). Google drive export: MSI + manual.
  - 00000008 (0.7, default) (works with MSI). Google drive export: analytical + manual, same hyperparameters.
  - 00000061 (default, default) (works with MSI). Google drive export: analytical + direct. 
  - 00000007 (default, default) (works with MSI)
  - 00000019 (default, default). Mesh intersection algorithm does not work well for Part 4 of this model - neighboring clusters, but actually
    it's a cylindrical surface split into two clusters for which the underlying primitive is the same. Works well with second round of vertex merging
    though... MSI does not work for tangent geometries, will handle those eventually as well. Google drive export: analytical + manual
  - 00000009 (default, default). Fitted 2 INRs instead of 2 cones, but the result was geometrically correct, and it visually matched the input reasonabl well.  
    Google drive export: analytical + manual, fails. Generation of oversized.step also failed, if the algorithm is constrained to primitive
    surfaces only it would have likely succeeded. Will not include it in final results, this one needs to be inspected, even mesh clipping
    seems to fail...
  - 00000017 (default, default)
  - 00000239 - Does not work, BSpline INR produced error seems to be causing the main problems. Google drive export: MSI + direct, algorithm 
    fails for parts (0, 1, 3), oversized and uv_alpha files for this model are missing, issues with freeform surfaces.
  - 00000073 - Does not work, cylinder/plane issues.... Actually, the issue seems to be genuine surface patches being split into multiple segments. Can you filter out such models entirely?
               Google drive export: MSI + direct, fails for part 1.
  - 00000077 (default, default). Google drive export: MSI + direct
  - 00000104 - Does not work, 0 vertices, simple model in that regard. Lowering the spacing factor to as low as 0.1 did produce better curves,
    but still not correct results. Sphere/cylinder intersection return 4 BSpline curves that should constitute a single closed curve,
    the algorithm breaks... With mesh intersections, works fine when reducing spacing_factor to 0.1, but still FreeCAD rejects it because of 
    "Invalid tolerance values". Try to catch these cases? Questionable even if such models could appear in practice?
     Google drive export: MSI + direct. Algorithm fails.
  - 00000121 (default, default). Good showcase of the algorithm for tomorrow's presentation.
    Google drive export: MSI + direct
  - 00000078 - Solver fails to compute the intersection between a cylinder and a plane that should be intersecting. When using mesh intersections,
    these pairs are still problematic. With mesh intersections in particular, this could be sidesteped by increasing cylinder radii for the purpose
    of mesh generation only, but there are more problems with the mesh pathway (merging gaps in polylines, reliable vertex computation, ...). 
    With MSI approach, cannot battle with tangent geometries...
    Google drive export: MSI + direct.
  - 00000105 - Does not work..... Sphere mesh generation flawed somehow???? Google drive export: MSI + direct
  - 00000090 - Does not work..... Google drive export: MSI + direct
  - 00000359 - Pipeline does not work for single-surface clusters, not that problematic, but irritating nontheless.
  - 00000406 - Works, but sphere is not trimmed properly. Region selection problem, this is relevant to other models. Google drive export: MSI + direct
  - 00000655 - Works in principle, but also problems with sphere...
  
  19 STEP files exported. 53 parts supposedly. The algorithm does not work on:
  - 00000005, part 1
  - 00000239, parts 0, 1, 3
  - 00000073, part 1
  - 00000104, part 0
  - 00000078, part 0
  - 00000105, part 0
  - 00000090, part 0
  - 00000406, part 0
  - 00000655, part 0
  Failure rate: 12/53, I expect that it's MUCH HIGHER...
  
    python main.py --output_dir output_p2cad --visualize --visualize_id 0 - Demonstrate that even mesh-derived intersections produce noisy results.
------------------------------
Experiments:
- Inflation after grid trimming, try to replace grid trimming with triangle-level filtering on a mesh.
- OCC as main engine, fallback to mesh intersection if it fails. How to merge multiple close mesh polylines into a single curve?

- For next presentation:
  - Outline cases where the algorithm might fail: tangent geometries, showcase on 00000078. Abandoning analytical pathway completely,
    deriving topology from meshes purely.
  - Use ideas from papers like ComplexGen and PolyFit for optimal face selection: replace the greedy oracle filter with ILP which
    GUARANTEES a correct model.
  - If everything fails, use PolyFiy pipeline to obtain linear approximation model instead.
  - For ILP, both ComplexGen and PolyFit use the Gurobi solver, which requires a licence but is free for academic use-cases.
    For the future, discuss how to obtain this licence.
  - ComplexGen is actually a full pipeline, from unsegmented point cloud to BRep!
  - Will try to resolve tangent geometries with mesh operations.

- Done today: Previous paragraph + trying to remove manual wire builder and rely purely on OCC.

- 2 (vertical plane), 9 (tilted cylinder), 10 (straight cylinder), 13 (horizontal plane)
- 2 vertices in intersection of (2, 9), (2, 13)

❯ The manual pathaway works, I've already established that. Are there any references to this algorithm in the literature, where it is explained in detail?                              

Yes, this is a well-known algorithm in computational geometry. The core idea — at each vertex, pick the next edge by angular ordering (first CCW from the incoming direction) to trace
  face boundaries — is the standard algorithm for computing faces of a planar subdivision. It's equivalent to traversing a DCEL (Doubly-Connected Edge List) or half-edge data
structure.

Key references:

1. de Berg, van Kreveld, Overmars, Schwarzkopf — "Computational Geometry: Algorithms and Applications" (Springer). Chapter 2 covers the DCEL and how face traversal works by following
  "twin" and "next" half-edges. The angular ordering at vertices is exactly how the DCEL's next-pointers are defined.
2. Mäntylä — "An Introduction to Solid Modeling" (Computer Science Press, 1988). Directly about BRep construction. Covers Euler operators and how face loops (wires) are assembled
from edges, including the distinction between outer and inner loops.
3. Hoffmann — "Geometric and Solid Modeling" (Morgan Kaufmann, 1989). Chapter on boundary representations, including wire assembly from edge graphs on surfaces.
4. Preparata & Shamos — "Computational Geometry: An Introduction". Covers planar subdivisions and face enumeration.

The most directly applicable is Mäntylä for the BRep context, and de Berg et al. for the algorithmic foundations. Your implementation is essentially: embed the edge graph on the
surface, compute angular ordering at each vertex using the surface tangent plane, then extract face cycles by always following the first CCW neighbor — which is the textbook DCEL
face traversal adapted to curved surfaces.

-----------------------------------------------------------------------------------------------------------------------------------------------
For tomorrow's meeting:
  - Mesh-self intersection (MSI) was shown to be more robust for topology extraction that intersecting adjecent meshes.

  - BRep assembly is extremely complicated, out of 22 models tested so far, the algorithm works on 12 (~54 %), 
    low success rate, it's likely to get even worse as more models are introduced, not paper worthy, 
    I doubt I can make any more improvements. 
  
  - MSI seems like the cleanest way to extract topology, but it's not a perfect method either, cannot cope with tangent geometries. 
    With mesh method, we handler tangent geometries, but there are geometries where tangency curve between two surfaces is not a line 
    (00000105)
  
  - Point2CAD's mesh generation could be improved:
    finer mesh trimming + analytical solution for freeforms instead of INRs. Extend the set of surface primitives. Also explore the setting
    of generating a mesh + primitives from a non-segmented point cloud.

  - It's unrealistic to expect a fully primitive-segmented point cloud in pratcice, CGAL has E-RANSAC implementation that fits a lot of 
    primitives.

  - Towards STEP methods: 
      -- Primitive fitting (with segmentation or E-RANSAC), but every face has a single wire: UV bounds derived from the supporting cluster.
      -- PolyFit pipeline generates only planes, should be simple enough for the current BRep builder. 
      -- Construct a mesh from the unsegemnted point cloud (Poisson/Bal pivoting), convert to STEP somehow.
      -- NONE of so-called reverse engineering papers actually produce a STEP. ComplexGen, arguably the most complex algorithm stops
         at topology. For model visualizations they derive meshes, they do not even bother to construct a full BRep, even though they produce
         a toplogy.

  - We have to accept that fully automatic reverse engineering is an impossible task (at least with respect to my understanding and current 
    skill level)! Generate watertight mesh + decent enough STEP as a starting point for an engineer. 
    Add also the extracted topology, although it could be noisy due to tangent geometries...

  - BRep itself is not relevant for evaluating fidelity: we would sample meshse and compute Chamfer distance, or any other metric between 
    point cloud sets anyway...

- Reduce risk of curves not matching surface equations: for polyline in intersection of surfaces (A, B) project each point to UV of 
  both surfaces, backrpoject and average out.

- More on alpha-shapes (generalization of the convex hull): https://cgm.cs.mcgill.ca/~godfried/teaching/projects97/belair/alpha.html
-------------------------------------------------------
Mesh generation (mesh_pipeline.py, BFS vs Point2CAD style with libicgal as mesh self intersection resolution engine)
- 00470: (Freeform, TODO)
- 00949: Both work fine.
- 00000949 - Both work fine.
- 00000077: Both work fine.
- 00000005: BFS better than libicgal p2cad, but original p2cad produces results similar to BFS.
- 00000006: BFS better than ligicgal p2cad and faster. BFS compared to original p2cad is a bit better and less jagged.
- 00000008: p2cad libicgal works much better than BFS. Same holds for original p2cad. The problem is that both p2cad variants are slower.
            p2cad libicgal produces less jagged mesh than original p2cad.
- 00000061: Both work fine.
- 00000007: Both work fine.
- 00000104: p2cad libicgal actually produces a better mesh than BFS. Original p2cad libicgal produces also some spurious results?
            Most likely cause: openings in the bottom planar cluster, not something we expect to encounter in practice. Increasing UV grid
            sampling resolution in the data generation script (scripts/abc_preprocess.py) removed the gaps in the ground truth cluster, but
            libigcal p2cad method still produced visually more appealing result.

- BFS trimming of the mesh did not yield significant improvements over original Point2CAD logic, although the algorithm
  is simpler. If BFS becomes problematic, replace it entirely with original Point2CAD mesh trimming logic.

- What to try out for freeforms:
  - Divide the cluster by levels, for each level fit a Spline, reduced representation?
  - Try running RANSAC over the freeform cluster?
  - MLS into BSpline?

- UV alpha-shape mesh masking (TODO):
  Replace distance-based grid_trimming (threshold_multiplier + spacing) with UV alpha-shape masking
  for ALL surface types. For each cluster, project points to surface UV, compute alpha shape, keep
  only grid cells whose UV centroid falls inside the alpha shape. For primitives: analytical UV
  projection. For INR freeforms: encoder already provides UV coordinates. Optional small
  shapely.buffer() offset for overlap at intersections. Parameter-free (just alpha), eliminates
  oversized mesh problem that causes single-component clipping failures with libigl/CGAL.
  Currently libigl/CGAL doesn't fragment meshes at intersection boundaries (unlike pymesh), so
  oversized meshes cause inner+outer regions to remain as one component — both BFS and p2cad-style
  clipping fail to trim the outer region.

- Part indexing: follows FreeCAD alignment, not by part labels!
- Number of connected components != sum(per face number of compontents) in mesh_pipeline?
- For best description of BRep, look at the SolidGen paper.
- To fix in brep pipeline: correct outer wire selection, tangent geometries, ILP to always get valid BRep.

- Just abandon STEP generation completely... Focus on mesh, as far as STEP can go we can always produce oversized +
uv_alpha... Improve mesh for freeforms...

- Mesh grid resolution is another contribution: our pipeline runs at 200x200 (sphere/cone),
  200x100 (cylinder), 200 (plane/INR). Original Point2CAD ships at 120 (plane), 100x100 (sphere),
  60x100 (cylinder), 50x100 (cone), 100 (INR) — and CAN'T trivially be bumped because their
  bit_mapping_points_torch step allocates O(grid * |P|) memory. If they had pushed grid resolution
  in their own ablations they might have noticed the same quality gap, but their tessellation is
  reshape-based and structurally caps grid density. Worth mentioning explicitly in the paper.

--------------- ABC PARTS ------------------------------
- 00470 - tangent intersection curves
- 00417 - tangent surfaces
- 00475 - Showcase meshes, mine vs Point2CAD
- 40024 - Interesting freeform, perhaps good for INR experimentation?

Automatic Reconstruction of Topology Optimization to Editable CAD Models - What about this paper?
BrepGaussian - What about this paper?

Loss regularizer experiment: Do not forget weight annealing!